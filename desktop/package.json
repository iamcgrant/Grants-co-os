{
  "name": "grants-co-os-desktop",
  "version": "0.1.1",
  "private": true,
  "description": "Grants & Co OS desktop shell (Tauri) — web app remains canonical",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0"
  }
}
