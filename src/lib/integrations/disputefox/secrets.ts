/**
 * DisputeFox secret / env names. Document names only — never log values.
 * Do not regenerate the Fox API key.
 */

export const DISPUTEFOX_API_KEY_ENV = "DISPUTEFOX_API_KEY";

/** Known Zapier zap. Must stay OFF — this inbound path does not enable it. */
export const DISPUTEFOX_ZAP_ID = "374413762";
export const DISPUTEFOX_ZAP_ENABLED = false;
