# Grants & Co OS — production environment template
# Copy values into Vercel Project → Settings → Environment Variables (Production).
# Never commit real secrets. Exact names match repository source (docs/PRODUCTION_ENV.md).

# --- Core (required) ---
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/neondb?sslmode=require
AUTH_SECRET=GENERATE_WITH_openssl_rand_base64_48
GC_CRON_SECRET=GENERATE_WITH_openssl_rand_base64_48
# Vercel Cron also accepts Authorization: Bearer $CRON_SECRET — set equal to GC_CRON_SECRET
CRON_SECRET=SAME_AS_GC_CRON_SECRET
NEXT_PUBLIC_APP_URL=https://os.grantandconsultants.com
GC_ENV=production
GC_DESKTOP_URL=https://os.grantandconsultants.com
# BUILDX owns Vercel deploy/domain/Neon — agent sessions do not require VERCEL_TOKEN
GC_VERCEL_EXTERNAL=1

# --- Payments (Commas primary in spirit) ---
# Fanbasis has no API Keys page. Do not invent COMMAS_API_KEY.
PAYMENT_PROVIDER=mock
# GRANTS_PAY_INBOUND_WEBHOOK_SECRET=
# COMMAS_API_KEY=
# COMMAS_WEBHOOK_SECRET=
# COMMAS_ENVIRONMENT=sandbox
COMMAS_LIVE_CHARGES=false
# COMMAS_CREATOR_HANDLE=

# --- GHL / LeadConnector (copy into Vercel Production) ---
GHL_API_KEY=
GHL_LOCATION_ID=
# PIT must include conversations/message.write for SMS/email and phone-system.voice for dialer

# --- Telegram team inbox (not client comms) ---
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_TEAM_CHAT_IDS=

# --- Cognito Forms (official API — never commit) ---
# COGNITO_API_KEY=

# --- SmartCredit (public join URL — set in Vercel, not hardcoded) ---
# SMARTCREDIT_SPONSOR_URL=https://www.smartcredit.com/join/?pid=69411

# --- Deploy tooling (BUILDX / CI only — not required in Next.js runtime) ---
# VERCEL_TOKEN=
# VERCEL_ORG_ID=
# VERCEL_PROJECT_ID=prj_7k6wvDk7P2NziRrcYsw2yUlSpwCx

# --- Desktop download buttons (defaults already point at public GitHub release assets) ---
# GC_DESKTOP_RELEASES_URL=https://github.com/iamcgrant/Grants-co-os/releases/tag/desktop-v0.1.2
# GC_DESKTOP_MAC_URL=
# GC_DESKTOP_WIN_URL=
# GC_DESKTOP_LINUX_URL=
